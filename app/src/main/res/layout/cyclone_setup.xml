<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#101114"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="32dp">

    <ImageView
        android:layout_width="96dp"
        android:layout_height="96dp"
        android:contentDescription="@string/app_name"
        android:src="@drawable/logo" />

    <TextView
        android:id="@+id/cyclone_setup_status"
        android:layout_width="320dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="28dp"
        android:gravity="center"
        android:text="@string/cyclone_setup_preparing"
        android:textColor="#E9EAEC"
        android:textSize="15sp" />

    <ProgressBar
        android:id="@+id/cyclone_setup_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="320dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="18dp"
        android:max="100"
        android:indeterminate="true"
        android:indeterminateTint="#FFFFFF"
        android:progressTint="#FFFFFF"
        android:progressBackgroundTint="#33FFFFFF" />

    <TextView
        android:id="@+id/cyclone_setup_hint"
        android:layout_width="320dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:alpha="0.6"
        android:gravity="center"
        android:text="@string/cyclone_setup_hint"
        android:textColor="#E9EAEC"
        android:textSize="12sp" />

</LinearLayout>
